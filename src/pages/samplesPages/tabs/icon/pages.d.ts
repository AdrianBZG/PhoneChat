import { Platform } from 'ionic-angular';
export declare class TabIconContentPage {
    isAndroid: boolean;
    constructor(platform: Platform);
}
export declare class IconPage {
    rootPage: typeof TabIconContentPage;
    isAndroid: boolean;
    constructor(platform: Platform);
}
