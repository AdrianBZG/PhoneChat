import { Platform } from 'ionic-angular';
export declare class TabIconTextContentPage {
    isAndroid: boolean;
    constructor(platform: Platform);
}
export declare class IconTextPage {
    rootPage: typeof TabIconTextContentPage;
    isAndroid: boolean;
    constructor(platform: Platform);
}
